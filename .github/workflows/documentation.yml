name: Documentation

on:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install docs dependencies
        working-directory: docs-site
        run: npm ci

      - name: Typecheck Docusaurus site
        working-directory: docs-site
        run: npm run typecheck

      - name: Build Docusaurus site
        working-directory: docs-site
        run: npm run build

      - name: Verify search index
        working-directory: docs-site
        run: test -s build/search-index.json

      - name: Verify published schemas
        working-directory: docs-site
        shell: bash
        run: |
          for schema in ../share/versions/*/clarid-codebook-schema.json; do
            version=$(basename "$(dirname "$schema")")
            cmp "$schema" "build/schemas/$version/clarid-codebook-schema.json"
          done

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v4
        with:
          path: docs-site/build

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v5
